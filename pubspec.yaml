name: cross_channel
description: Fast & flexible channels for Dart/Flutter.
version: 0.6.0
repository: https://github.com/mkidv/cross_channel

topics:
  - concurrency
  - async
  - channels
  - mpsc
  - mpmc

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
